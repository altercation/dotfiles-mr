[$HOME/.config/vcsh/repo.d/vim.git]
checkout =
	if anon; then
		vcsh clone git://git@github.com:altercation/dotfiles-vim.git vim
	else
		vcsh clone ssh://git@github.com:altercation/dotfiles-vim.git vim
	fi
